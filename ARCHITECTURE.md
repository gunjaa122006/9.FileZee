# System Architecture

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                            │
│  (Browser, Mobile App, API Client, cURL, Postman, etc.)       │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP/HTTPS
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                    REVERSE PROXY (Optional)                     │
│                  (Nginx, Apache, Cloudflare)                    │
│  • TLS Termination                                              │
│  • Load Balancing                                               │
│  • Rate Limiting                                                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                     EXPRESS SERVER (Node.js)                    │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Middleware Stack                      │  │
│  │  1. Request Logging                                      │  │
│  │  2. Body Parser (JSON/URL-encoded)                      │  │
│  │  3. Multer (File Upload)                                │  │
│  │  4. Security Validators                                 │  │
│  │  5. Storage Limit Checker                               │  │
│  │  6. MIME Type Validator                                 │  │
│  │  7. Error Handler                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    API Routes                            │  │
│  │  POST   /api/upload        → Upload file                 │  │
│  │  GET    /api/files         → List files                  │  │
│  │  GET    /api/files/:id     → Get file metadata           │  │
│  │  GET    /api/download/:id  → Download file               │  │
│  │  DELETE /api/files/:id     → Delete file                 │  │
│  │  GET    /api/health        → Health check                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└────────────┬────────────────────────────────┬──────────────────┘
             │                                │
             │                                │
┌────────────▼────────────┐      ┌───────────▼──────────────────┐
│   LIFECYCLE SERVICE     │      │     METADATA STORE           │
│                         │      │                              │
│  • Auto Cleanup         │◄────►│  • File Metadata (JSON)      │
│  • Expiration Check     │      │  • Persistent Storage        │
│  • Orphan Detection     │      │  • CRUD Operations           │
│  • Scheduled Jobs       │      │                              │
└────────────┬────────────┘      └───────────┬──────────────────┘
             │                                │
             │                                │
┌────────────▼────────────────────────────────▼──────────────────┐
│                       FILE SYSTEM                              │
│                                                                 │
│  ┌─────────────────┐              ┌────────────────────────┐   │
│  │  uploads/       │              │  metadata/             │   │
│  │  • file1.pdf    │              │  • files.json          │   │
│  │  • file2.jpg    │              │    (metadata records)  │   │
│  │  • file3.docx   │              │                        │   │
│  └─────────────────┘              └────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow

### 1. File Upload Flow

```
Client
  │
  │ 1. POST /api/upload (multipart/form-data)
  ▼
Express Server
  │
  │ 2. Multer Middleware
  │    • Check file extension
  │    • Validate MIME type (claimed)
  │    • Check file size
  │    • Generate unique filename
  ▼
Temporary Storage
  │
  │ 3. Security Middleware
  │    • Read file content (magic bytes)
  │    • Verify actual MIME type
  │    • Check storage quota
  ▼
Validation
  │
  │ 4. If valid → continue
  │    If invalid → delete file, return error
  ▼
File System (uploads/)
  │
  │ 5. Save file permanently
  ▼
Metadata Store
  │
  │ 6. Create metadata record
  │    • File ID (unique filename)
  │    • Original name
  │    • Size, MIME type
  │    • Upload timestamp
  │    • Expiration timestamp
  ▼
Response to Client
  │
  │ 7. Return file metadata
  └─► { fileId, originalName, size, uploadedAt, expiresAt }
```

### 2. File Download Flow

```
Client
  │
  │ 1. GET /api/download/:fileId
  ▼
Express Server
  │
  │ 2. Validate file ID
  │    • Sanitize input
  │    • Prevent path traversal
  ▼
Metadata Store
  │
  │ 3. Lookup file metadata
  │    • Find by file ID
  │    • Return file info
  ▼
File System Check
  │
  │ 4. Verify file exists on disk
  │    • If not → clean metadata, return 404
  │    • If exists → continue
  ▼
Stream File
  │
  │ 5. Set response headers
  │    • Content-Type
  │    • Content-Disposition (download)
  │    • Content-Length
  ▼
Response to Client
  │
  │ 6. Stream file data
  └─► Binary file content
```

### 3. File Cleanup Flow

```
Scheduled Job (Every CLEANUP_INTERVAL_HOURS)
  │
  │ 1. Trigger cleanup
  ▼
Lifecycle Service
  │
  │ 2. Find expired files
  │    • Query metadata store
  │    • Filter by expiration date
  ▼
Delete Expired Files
  │
  │ 3. For each expired file:
  │    • Delete from file system
  │    • Remove metadata record
  ▼
Clean Orphaned Metadata
  │
  │ 4. Find metadata without files
  │    • Check file existence
  │    • Remove orphaned records
  ▼
Clean Orphaned Files
  │
  │ 5. Find files without metadata
  │    • List files in uploads/
  │    • Delete untracked files
  ▼
Log Results
  │
  └─► Cleanup completed: X files deleted
```

---

## 🔒 Security Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                     SECURITY LAYERS                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Layer 1: Network Security (Optional - Production)             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  • Firewall rules                                        │  │
│  │  • TLS/HTTPS encryption                                  │  │
│  │  • Rate limiting                                         │  │
│  │  • DDoS protection                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Layer 2: Application Security                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  • Authentication (add if needed)                        │  │
│  │  • Authorization (add if needed)                         │  │
│  │  • Session management                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Layer 3: Input Validation                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ✓ File extension checking                               │  │
│  │  ✓ MIME type validation (claimed)                        │  │
│  │  ✓ File size validation                                  │  │
│  │  ✓ Filename sanitization                                 │  │
│  │  ✓ Path traversal prevention                             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Layer 4: Content Validation                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ✓ Magic byte detection (actual MIME type)               │  │
│  │  ✓ MIME type verification                                │  │
│  │  ✓ Allowed types whitelist                               │  │
│  │  • Virus scanning (add if needed)                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Layer 5: Storage Protection                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ✓ Storage quota enforcement                             │  │
│  │  ✓ File size limits                                      │  │
│  │  ✓ Unique filename generation                            │  │
│  │  ✓ No internal path exposure                             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Layer 6: Lifecycle Security                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  ✓ Automatic file expiration                             │  │
│  │  ✓ Scheduled cleanup                                     │  │
│  │  ✓ Orphaned resource cleanup                             │  │
│  │  ✓ Metadata synchronization                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

Legend:
  ✓ = Implemented
  • = Recommended for production
```

---

## 📊 Data Flow Diagram

```
┌──────────┐
│  Client  │
└────┬─────┘
     │
     │ File Upload Request
     ▼
┌──────────────────────┐
│  Multer Middleware   │
│  • Parse multipart   │
│  • Validate basics   │
│  • Save to temp      │
└────┬─────────────────┘
     │
     │ File Data
     ▼
┌─────────────────────────┐
│  Security Middleware    │
│  • Magic byte check     │
│  • Storage quota check  │
│  • MIME verification    │
└────┬────────────────────┘
     │
     │ Validated File
     ▼
┌─────────────────────┐          ┌──────────────────┐
│  File System        │◄─────────│  Metadata Store  │
│  • Save file        │          │  • Create record │
│  • uploads/         │          │  • files.json    │
└─────────────────────┘          └──────────────────┘
     │                                    │
     │                                    │
     └────────────────┬───────────────────┘
                      │
                      │ File Metadata
                      ▼
                 ┌─────────┐
                 │ Response│
                 └─────────┘
```

---

## 🔄 Component Interactions

```
┌─────────────┐
│   Server    │
│  (Entry)    │
└──────┬──────┘
       │
       │ Initializes
       ▼
┌─────────────────────────────────────────────────────┐
│                    Components                       │
│                                                     │
│  ┌──────────┐  ┌────────────┐  ┌───────────────┐  │
│  │  Config  │  │   Multer   │  │   Metadata    │  │
│  │          │  │            │  │    Store      │  │
│  └────┬─────┘  └─────┬──────┘  └───────┬───────┘  │
│       │              │                  │          │
│       │              │                  │          │
│  ┌────▼──────────────▼──────────────────▼───────┐  │
│  │              Routes Layer                    │  │
│  │  • Upload    • List    • Download • Delete  │  │
│  └─────┬────────────┬─────────────┬──────┬─────┘  │
│        │            │             │      │        │
│        │            │             │      │        │
│  ┌─────▼────────────▼─────────────▼──────▼─────┐  │
│  │           Middleware Stack                  │  │
│  │  • Security  • Validation  • Error Handler │  │
│  └─────────────────────┬────────────────────────┘  │
│                        │                           │
└────────────────────────┼───────────────────────────┘
                         │
                         │
                    ┌────▼────┐
                    │ Response│
                    └─────────┘

Background Process:
┌──────────────────┐
│ Lifecycle Service│
│  (Scheduled)     │
└────────┬─────────┘
         │
         │ Every CLEANUP_INTERVAL_HOURS
         ▼
    ┌─────────────────┐
    │  Cleanup Job    │
    │  • Expired      │
    │  • Orphaned     │
    └─────────────────┘
```

---

## 💾 Data Model

### File Metadata Structure

```javascript
{
  id: "document_1737281400000_abc123def456.pdf",  // Unique identifier
  originalName: "document.pdf",                   // User's filename
  filename: "document_1737281400000_abc123def456.pdf", // Stored filename
  mimeType: "application/pdf",                    // MIME type
  size: 102400,                                   // Size in bytes
  path: "/uploads/document_1737281400000_abc123def456.pdf", // Full path
  uploadedAt: "2026-01-19T10:30:00.000Z",        // Upload timestamp
  expiresAt: "2026-01-20T10:30:00.000Z"          // Expiration timestamp
}
```

### Metadata Storage (JSON)

```
metadata/
└── files.json
    [
      { metadata record 1 },
      { metadata record 2 },
      { metadata record 3 },
      ...
    ]
```

---

## 🚦 State Diagram

```
                    ┌──────────┐
                    │  Client  │
                    │  Upload  │
                    └────┬─────┘
                         │
                         ▼
                  ┌──────────────┐
                  │  Validating  │
                  └──────┬───────┘
                         │
              ┌──────────┴──────────┐
              │                     │
         ┌────▼─────┐          ┌───▼────┐
         │  Valid   │          │Invalid │
         └────┬─────┘          └───┬────┘
              │                    │
              ▼                    ▼
         ┌─────────┐          ┌────────┐
         │ Stored  │          │Rejected│
         └────┬────┘          └────────┘
              │
              ▼
         ┌─────────┐
         │  Active │◄────────┐
         │  (Can   │         │
         │ access) │         │
         └────┬────┘         │
              │              │
              │              │ Manual
    Retention │              │ Access
    Period    │              │
    Expires   │              │
              ▼              │
         ┌─────────┐         │
         │ Expired │         │
         └────┬────┘         │
              │              │
              │ Cleanup Job  │
              ▼              │
         ┌─────────┐         │
         │ Deleted │─────────┘
         └─────────┘
              │
              │ Gone forever
              ▼
           [ End ]
```

---

## 📈 Scalability Considerations

### Current Architecture
- ✅ **Single Node**: Works well for small to medium workloads
- ✅ **Local Storage**: Simple, fast, no external dependencies
- ✅ **JSON Metadata**: Lightweight, no database needed

### Scaling Strategies

#### Vertical Scaling (Single Server)
```
Current: 1 Node.js process
         ↓
Scale:   Cluster mode (CPU cores)
         ↓
Scale:   More RAM, faster disk
```

#### Horizontal Scaling (Multiple Servers)
```
Multiple Servers
      ↓
Need: Shared Storage (NFS, S3, Azure Blob)
      +
      Database (PostgreSQL, MongoDB)
      +
      Load Balancer (nginx, HAProxy)
```

### Migration Path to Cloud

```
Phase 1: Local Storage (Current)
    uploads/ → Local disk
    metadata/ → Local JSON

Phase 2: Cloud Storage
    uploads/ → AWS S3 / Azure Blob
    metadata/ → Local JSON

Phase 3: Full Cloud
    uploads/ → AWS S3 / Azure Blob
    metadata/ → PostgreSQL / MongoDB
    + Multiple app instances
    + Load balancer
```

---

## 🎯 Design Decisions

### Why These Technologies?

**Express**
- ✅ Mature, widely used
- ✅ Extensive middleware ecosystem
- ✅ Great for REST APIs

**Multer**
- ✅ De facto standard for file uploads
- ✅ Stream-based (efficient)
- ✅ Flexible configuration

**file-type**
- ✅ Magic byte detection
- ✅ Prevents MIME spoofing
- ✅ Lightweight

**JSON for Metadata**
- ✅ No database needed
- ✅ Simple to understand
- ✅ Easy to backup
- ⚠️  Migrate to DB for scale

---

**This architecture is designed for production use while remaining simple to understand and deploy.**
